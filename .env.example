# Copy to .env.local and add your key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-key-here

# For Vercel: Project → Settings → Environment Variables → OPENAI_API_KEY
# Apply to Production (and Preview if you use preview deployments), then redeploy.
